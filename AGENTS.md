# WriteX Agent 协作规则

WriteX 采用 Codex 主控、DeepSeek Harness（DSH）执行的协作开发方式。

## 强制规则

- 同一功能分支、同一开发阶段复用一个持久 DSH 会话。
- 不得为单个文件、单个测试失败或一次小返修创建新的 DSH 会话。
- 上下文变长时先 compact；只有 compact 后仍不适用、任务明显无关、会话状态污染或需要独立盲审时才新建会话。
- Codex 主控负责范围拆分、架构决策、规范审查、代码质量审查、最终验证和 Git 操作。
- DSH 执行者负责实现、定向测试、自检和按集中反馈返修。
- DSH 不得提交、合并、推送或修改 Git 元数据。
- 一个工作树同时只能有一个写入型 DSH 会话。
- 相关任务应组成可测试的垂直切片后一次派发，避免重复发送完整背景和计划。
- 反馈必须由主控集中整理后发回原会话；不得为每条审查意见分别新建会话。
- 真实模型、提示词版本、token、成本和缓存指标必须进入运行记录。

完整协议见：

`docs/superpowers/specs/2026-09-03-persistent-dsh-development-protocol-design.md`
